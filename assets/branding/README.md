# Deadline Dock branding

Created 2026-09-15 using the built-in image generation tool.

- `deadline-dock-logo.png`: original generated artwork (1254 × 1254).
- `deadline-dock-oauth-120.png`: 120 × 120 PNG export for Google OAuth; same design resized using Lanczos.
- The portal uses the same OAuth export. These assets do not change the already published v0.1.11 executable icon.

Generation prompt:

> Create a production-ready square app logo for Deadline Dock, a calm Japanese Windows task management application. Single centered bold geometric uppercase D monogram cleverly integrated with a simple checkmark, cream/off-white symbol on a solid deep forest green (#303d32) rounded-square tile, subtle muted sage accent at most. Flat vector-like crisp geometry, no gradients, no shadows, no 3D, no texture, no surrounding mockup, no extra text or wordmark, no trademark symbols. Balanced generous margins, high contrast, clear at 32px. Square PNG suitable for Google OAuth app branding. The artwork should fill the square canvas with the rounded tile and a clean white outer background at corners. Output one finished logo only.
