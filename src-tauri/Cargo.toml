[package]
name = "deadline-dock"
version = "0.1.10"
description = "Deadline-first local task ledger"
authors = ["Deadline Dock"]
edition = "2021"

[lib]
name = "deadline_dock_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-sql = { version = "2", features = ["sqlite"] }
tauri-plugin-opener = "2"
tauri-plugin-dialog = "2"
tauri-plugin-global-shortcut = "2"

tauri-plugin-fs = "2"
tauri-plugin-window-state = "2"

# Required by tauri::generate_context!() generated code.
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
tokio = { version = "1", features = ["net", "io-util", "time", "sync"] }
sqlx = { version = "0.8", features = ["sqlite", "runtime-tokio"] }
keyring = { version = "3", features = ["windows-native"] }
rand = "0.8"
sha2 = "0.10"
base64 = "0.22"
url = "2"
chrono = "0.4"
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
http = "1"

# Keep local Windows builds small; full symbols can be enabled explicitly when debugging.
[profile.dev]
debug = 0
incremental = false
